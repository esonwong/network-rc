{
  "name": "network-rc",
  "version": "0.0.1",
  "description": "网络控制遥控车",
  "main": "index.js",
  "repository": "git@github.com:itiwll/network-rc.git",
  "author": "Eson Wong <itiwll@gmail.com>",
  "license": "MIT",
  "scripts": {
    "start": "nodemon index.js",
    "server": "node index.js",
    "build": "rm -fr dist && mkdir dist && cp index.js dist/ && cp -r node_modules dist/ && mkdir dist/front-end && cp -r front-end/build dist/front-end/"
  },
  "dependencies": {
    "@clusterws/cws": "^1.5.0",
    "express": "^4.17.1",
    "rpio": "^2.1.1",
    "ws-avc-player": "^2.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
